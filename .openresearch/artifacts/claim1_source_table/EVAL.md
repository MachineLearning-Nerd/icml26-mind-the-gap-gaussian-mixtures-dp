# Baseline evaluator entry

Current evidence status: **BLOCKED** for scientific verification.

The committed verifier establishes that the exact Claim 1 contract is machine-readable and fail-closed. It does not reproduce the mechanism outputs. The first child experiment supersedes this page once full recalibration evidence exists.

Fixed command:

```bash
uv sync --frozen && .venv/bin/python repro/run_all.py
```
