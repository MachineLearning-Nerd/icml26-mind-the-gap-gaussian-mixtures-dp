# Claim 2 — VERIFIED

**Exact claim.** For every δ∈(0,½), there exists ε₀>0 such that `L₂^MG(ε,δ) < L₂^AG(ε,δ)` for all ε≥ε₀ (Proposition 3.3, `#S3.Thmtheorem3`; proof `#A2.SS5`). Sensitivity is positive and losses are noise powers.

The previous live judge awarded full credit for the preserved tail-regime check. The cumulative suite reruns it across the 15 Table 1 δ values and ε∈{5,10}: 30/30 strict variance wins. This finite grid is regression evidence, not a replacement for the universal proof; the proposition and its appendix proof remain the proof-level basis.

Raw regression: [claim JSON](../../evidence/current/claim2_proposition_33_regression.json). Executable path: [run_all.py](../../code/current/repro/run_all.py). The same seed, lock, command, HF allocation, and nonzero-exit policy stated on [Current verification](#/current-verification) apply.

**Control and limit.** The negative control replaces one strict variance win with equality, reducing the count to 29/30; it is rejected. The scope is explicitly finite, so no new universal empirical claim is made. The historical full-credit evidence remains reachable under **Historical rejected baseline**.
